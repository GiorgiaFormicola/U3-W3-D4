const initialState = {
  jobs: [],
};

const searchReducer = (currentState = initialState, action) => {
  switch (action.type) {
    default:
      return currentState;
  }
};

export default searchReducer;
